[project]
name = "python-project-template"
version = "0.1.0"
description = "python-project-template"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ruff",
    "pylint",
    "beautysh",
    "setuptools"
]

